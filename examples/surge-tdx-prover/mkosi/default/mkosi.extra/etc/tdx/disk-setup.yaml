disks:
  disk_persistent:
    strategy: "pathglob"
    strategy_config:
      pattern: "/dev/vda3"
    format: "on_fail"
    encryption_key: "key_persistent"
    mount_at: "/persistent"
    dirs: ["ssh", "data", "logs"]
